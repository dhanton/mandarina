//DoStatus(type, json_id)

DoStatus(STUNNED, "stunned")
DoStatus(SILENCED, "silenced")
DoStatus(DISARMED, "disarmed")
DoStatus(ROOTED, "rooted")
DoStatus(SLOWED, "slowed")
DoStatus(MEAT_SHIELD, "meat_shield_status")
